---
description: Electron客户端国内化交付与激活流程规则（默认不做后端）
alwaysApply: true
---

# Electron 客户端交付规则（可复用）

适用于需要把项目打包给非技术用户安装的场景，默认仅做客户端侧，不新增后端系统。

## 目标
- 产出可安装包（优先 Windows，按需加 macOS）。
- 安装器阶段必须先做激活码校验，校验通过才允许继续安装。
- 安装完成后客户端默认仅做配置管理，不在客户端重复做激活流程。
- 配置引导优先读取项目 `.env.example`，生成清晰的“必填/推荐”填写流程。

## 必做项
- 使用国内可访问依赖源（Python/Node/包管理器），避免国外源不可达。
- 增加环境检测：检查 Python/Node/pip/npm/uv 等版本与可执行路径。
- 提供自动修复入口：版本不符或依赖源异常时可自动修复并给出日志。
- 安装与首启流程可视化：显示步骤、进度、失败原因与下一步建议。

## 激活与重装策略
- 首次安装要求在安装器界面输入激活码（不是应用首屏输入）。
- 激活成功后本地保存可验证凭证（签名 license 或等效安全凭证）。
- 安装器在要求输入激活码前，必须先检查本机授权缓存（如 license 文件/注册表/受保护本地存储）；命中且有效时可跳过输入步骤。
- 同一设备卸载重装时，允许幂等恢复，不应再次要求输入激活码。
- 禁止把服务端密钥或签名私钥放入客户端。

## 激活安全要求（防拦截与伪造）
- 禁止仅以“后端返回 true/false”作为激活成功依据；必须返回可验签的许可证数据。
- 激活接口必须使用挑战响应：客户端先发送 `nonce` + `timestamp` + `deviceId` 摘要，服务端将这些字段写入签名载荷后返回。
- 安装器激活请求必须带 `productCode` 与 `clientType`，服务端必须校验与激活码绑定类型一致。
- 客户端仅保存服务端签名的 license，离线校验签名、公钥、时间窗口、设备绑定后才视为激活成功。
- 必须防重放：`nonce` 一次性使用、`timestamp` 过期拒绝、同请求 ID 幂等处理。
- 传输层必须启用 HTTPS 和证书校验（可选证书固定 pinning），防中间人替换响应。
- 本地可加密存储 license，但“加密”不是信任根；真正防伪造依赖服务端签名验真。
- 安装器联网激活必须采用失败即拒绝安装（fail-closed）策略，网络异常/超时不得默认放行。

## 范围约束
- 默认不实现后端管理系统（如 key 管理后台、创建接口、登录系统）。
- 若用户明确要求后端，需先单独确认部署方式、安全策略和数据库选型再实施。

## 配置引导展示要求
- 客户端必须展示 `.env.example` 中每个变量对应的注释说明，不能只显示变量名。
- 注释中的“用途、获取方式、注意事项、获取地址链接”等信息应尽量结构化展示。
- 变量应按 `required/recommended/optional` 分组并提供明显标识，优先引导用户先填必需项。
- 配置界面至少提供“仅显示必填项”的筛选能力，并支持链接点击跳转。

## 国内源与打包要求
- 不仅依赖安装走国内源，Electron 打包下载也必须使用国内镜像。
- Windows/macOS 打包脚本应显式配置镜像变量（如 `ELECTRON_MIRROR`、`ELECTRON_BUILDER_BINARIES_MIRROR`），避免访问 GitHub 失败导致构建中断。

## 交付标准
- 普通用户无需命令行即可完成安装、激活、配置与启动。
- 国内网络环境下可完成依赖安装与启动。
- 激活失败、网络失败、环境异常都有明确错误提示与可执行修复建议。
- 验收至少覆盖：新设备首装需激活、同设备重装免输码、异设备复用同码被拒绝、断网安装被阻断、配置注释与链接展示正确。
