# 应用基础路由，默认值为 "/"
# 如果部署在子目录，必须以 "/" 结尾，例如 "/admin/"
VITE_BASE_URL=/
VITE_API_BASE_URL=http://localhost:3000/api

VITE_APP_TITLE=星帆中控台

VITE_APP_DESC=StarSail Console is a creative and efficient management platform

# 图标名称前缀
VITE_ICON_PREFIX=icon

# 本地 SVG 图标组件前缀，必须包含 VITE_ICON_PREFIX
# 格式：{VITE_ICON_PREFIX}-{本地图标名}
VITE_ICON_LOCAL_PREFIX=icon-local

# 权限路由模式：static（静态）｜dynamic（动态）
VITE_AUTH_ROUTE_MODE=dynamic


# 静态路由模式下的首页路由名
VITE_ROUTE_HOME=home

# 默认菜单图标
VITE_MENU_ICON=mdi:menu

# 开发模式是否启用 HTTP 代理
VITE_HTTP_PROXY=N

# Vue Router 模式：hash | history | memory
VITE_ROUTER_HISTORY_MODE=history

# 后端成功状态码，命中后视为请求成功
VITE_SERVICE_SUCCESS_CODE=0000

# 后端登出状态码，命中后会退出登录并跳转登录页
VITE_SERVICE_LOGOUT_CODES=8888,8889

# 后端弹窗登出状态码，命中后以弹窗方式提示并登出
VITE_SERVICE_MODAL_LOGOUT_CODES=7777,7778

# Token 过期状态码，命中后会刷新 Token 并重试请求
VITE_SERVICE_EXPIRED_TOKEN_CODES=9999,9998,3333

# 静态路由模式下的超级角色标识
VITE_STATIC_SUPER_ROLE=R_SUPER

# 用于区分不同域名下的本地存储前缀
VITE_STORAGE_PREFIX=SOY_

# 是否自动检测更新
VITE_AUTOMATICALLY_DETECT_UPDATE=Y
